apiVersion: v1
kind: Service
metadata:
  name: cintent-api
  namespace: cintent
spec:
  selector:
    app: cintent-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
